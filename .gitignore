node_modules
storybook-static
*.log
.DS_Store
.env
.env.local
example-app/.expo
example-app/node_modules
example-app/storybook.requires.ts
coverage

# Local build artifacts consumed via file: protocol — not versioned.
*.tgz
